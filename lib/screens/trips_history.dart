import 'package:flutter/material.dart';

class TripsHistory extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Container(
      child: Center(child: Text('Trips History', style: TextStyle(fontSize: 40),)),
    );
  }
}